__version__ = '0.0.1'

import torch
import torch_complex.cpp
# import importlib

# # pretend to be pytorch
# __globals__ = globals()
# for each in dir(torch):
#     __globals__[each] = getattr(torch, each)
